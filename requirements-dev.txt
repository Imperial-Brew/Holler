# Test-only dependencies. Install with:  pip install -r requirements-dev.txt
# (production does NOT install these — see requirements.txt for the app itself)
-r requirements.txt

pytest>=8,<9
pytest-asyncio>=0.24,<0.25
httpx>=0.27,<0.29
testcontainers[postgres]>=4,<5
