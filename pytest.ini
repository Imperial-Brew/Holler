[pytest]
# Treat every `async def test_...` as an async test automatically.
asyncio_mode = auto
testpaths = tests
# Put the repo root on the import path so tests can `import main` / `import app`.
pythonpath = .
